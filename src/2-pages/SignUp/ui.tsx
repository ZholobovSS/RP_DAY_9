import { SignUpForm } from '@/4-features/SignUp';
import { Box } from '@mui/material';
import type { FC } from 'react';

export const SignUp: FC = () => {
    return (
        <Box>
            <SignUpForm />
        </Box>
    );
};
