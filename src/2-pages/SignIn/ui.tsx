import { SignInForm } from '@/4-features/SignIn';
import { Box } from '@mui/material';
import type { FC } from 'react';

export const SignIn: FC = () => {
    return (
        <Box>
            <SignInForm />
        </Box>
    );
};
